#! /Users/adam/projects/sound/yalce/build/lang
%include Fmt
%include Synth
%include Math

let schedule_event = extern fn Ptr -> Ptr -> Double -> ();
let handle_cb = extern fn Ptr -> Ptr -> ();

let x = sq_node 50. + sq_node 50.5;
x |> play_node;

let f = fn () ->
  set_input_scalar x 0 (rand_double_range 50. 200.);
  schedule_event f (1, 2) 0.25
;;

schedule_event f (1, 2) 1.0;

# f |> sq_node


