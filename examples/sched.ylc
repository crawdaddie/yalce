let init_audio = extern fn () -> ();

init_audio ();

type Timer = Double * Int;

let schedule_event = extern fn Ptr -> Ptr -> UInt64 -> Double -> ();

let x = fn now ->
  # fprintf stderr "trig x\n";
  schedule_event x () now 1.
;;

x 0;

