(executables
 (names main 
        ; ergonomic
        clicks_cuts polyphony acid acid_phase test test_snare)
 (preprocess (pps lwt_ppx))
 (libraries core yalce unix raylib ctypes-foreign core_unix.command_unix portmidi lwt.unix)
)
