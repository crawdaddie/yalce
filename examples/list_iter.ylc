let list_sum = fn s l ->
  (match l with
  | [] -> s
  | x::rest -> list_sum (s + x) rest
  )
;;

# let list_match = fn l ->
#   (match l with
#   | [1,2,3] -> 0
#   | [] -> 100
#   )
# ;;

let sum = list_sum 0

