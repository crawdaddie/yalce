let getenv = extern fn Ptr -> Ptr;
let strlen = extern fn Ptr -> Int;
let from_cstr = extern fn Int -> Ptr -> String;

let get_env = fn e ->
  match getenv (cstr e) with
  | p if (is_null p == false) -> Some (from_cstr (strlen p) p)
  | _ -> None
;;
