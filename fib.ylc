%include examples/String

let fib = fn x ->
  match x with
  | 0 -> 0
  | 1 -> 1
  | _ -> (fib (x - 1)) + (fib (x - 2))
;;

# println `hello? {fib 20}`;
#
let sum = fn a b -> a + b;;
let proc = fn f a b -> f a b;;

# println `{proc sum 1 2} == 3?`;
proc sum 1.0 2.0;
proc sum 1 2;

println `{proc sum 1 2} == 3?`;
println `{proc sum 1. 2.} == 3.?`;
